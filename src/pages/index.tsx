import { type NextPage } from "next";

import { FetchData } from "~/seed/fetchDate/fetchData";
import { Layout } from "~/components";

const Home: NextPage = () => {
  return (
    <Layout title="Dashboard">
        {/* <FetchData/> */}
    </Layout>
  );
};

export default Home;
